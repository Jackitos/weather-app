---
const { day, high, low } = Astro.props;
---

<div
	class="flex flex-col justify-center items-center gap-2 bg-[rgb(255,255,255,0.03)] p-5 rounded-lg text-white text-sm backdrop-blur-md border border-white border-opacity-10 hover:border-opacity-20 transition-all duration-300 cursor-pointer select-none hover:scale-105 transform origin-center hover:shadow-lg hover:bg-[rgb(255,255,255,0.05)] hover:text-white/80 hover:font-bold"
>
	<i class="fa-solid fa-cloud flex justify-center"></i>
	<p>{day}</p>
	<div class="flex flex-row text-sm gap-3">
		<div class="flex flex-row">
			<p>h:</p>
			<p>{high}°</p>
		</div>
		<div class="flex flex-row">
			<p>l:</p>
			<p>{low}°</p>
		</div>
	</div>
</div>
